"use strict";
/**
 * 
 * @param {*} nums 
 * 第一道题目
 * 找到数组中符合条件
 * a+b+c=0 的三个数
 * 并且返回
 * 例：
 *   输入
 *   [-2,-1,0,1,2]
 * 
 *   输出
 *   [
 *   [-2,0,2],
 *   [-1,0,1]
 *   ]
 */
module.exports = function (nums) {
    let result = [-1,0,1];
    //TODO

    return result;
}